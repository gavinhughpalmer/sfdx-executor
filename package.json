{
  "name": "sfdx-executor",
  "description": "This plugin is to execure series of sfdx commands simplifiying tasks that require a number of commands to be executed",
  "version": "0.0.0",
  "author": "Gavin Palmer",
  "bugs": "https://github.com/gavinhughpalmer/sfdx-executor/issues",
  "dependencies": {
    "@oclif/command": "1.8.0",
    "@oclif/config": "1.17.0",
    "@oclif/errors": "1.3.3",
    "@salesforce/command": "2.2.0",
    "@salesforce/core": "2.11.0",
    "tslib": "1.13.0"
  },
  "devDependencies": {
    "@oclif/dev-cli": "1.22.2",
    "@oclif/plugin-help": "3.2.0",
    "@oclif/test": "1.2.6",
    "@salesforce/dev-config": "1.6.0",
    "@salesforce/ts-sinon": "1.2.2",
    "@types/chai": "4.2.12",
    "@types/mocha": "5.2.7",
    "@types/node": "10.17.28",
    "chai": "4.2.0",
    "globby": "8.0.2",
    "mocha": "5.2.0",
    "nyc": "14.1.1",
    "sfdx-cli": "7.72.0",
    "ts-node": "8.10.2",
    "tslint": "5.20.1"
  },
  "engines": {
    "node": ">=8.0.0"
  },
  "files": [
    "/lib",
    "/messages",
    "/npm-shrinkwrap.json",
    "/oclif.manifest.json"
  ],
  "homepage": "https://github.com/gavinhughpalmer/sfdx-executor",
  "keywords": [
    "sfdx-plugin"
  ],
  "license": "MIT",
  "oclif": {
    "commands": "./lib/commands",
    "bin": "sfdx",
    "topics": {
      "hello": {
        "description": "Commands to say hello."
      }
    },
    "devPlugins": [
      "@oclif/plugin-help"
    ]
  },
  "repository": "gavinhughpalmer/sfdx-executor",
  "scripts": {
    "lint": "tslint --project . --config tslint.json --format stylish",
    "postpack": "rm -f oclif.manifest.json",
    "posttest": "tslint -p test -t stylish",
    "prepack": "rm -rf lib && tsc -b && oclif-dev manifest && oclif-dev readme",
    "test": "nyc --extension .ts mocha --forbid-only \"test/**/*.test.ts\"",
    "version": "oclif-dev readme && git add README.md",
    "fix": "tslint -p test -t stylish --fix"
  }
}
